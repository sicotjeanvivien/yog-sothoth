# Rust
/target

# WASM
/crates/wasm/pkg

# Node.js / Next.js
web/node_modules
web/.next
web/.env
web/.env.local
web/.env.*.local

# Docker
.env

# OS
.DS_Store
Thumbs.db
# Métadonnée Windows « ce fichier vient d'Internet », attachée en flux annexe
# invisible sous Windows — mais que WSL expose comme un fichier à part entière,
# qu'un `git add .` ramasse sans que personne le voie. Trois s'étaient glissés
# dans web/public/ avec ddd3506.
*:Zone.Identifier

# IDE
.vscode/
.idea/
*.swp

/log

# Project workflow / backlog — lives in its own private repo
.project/